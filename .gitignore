# dependencies
node_modules/
# env & secrets
.env
*.env
!.env.example
# build output
dist/
build/
# prisma
server/prisma/*.db
# logs & OS
*.log
.DS_Store

# uploaded attachments (Issue #13)
server/uploads/

# vite dependency pre-bundle cache
.vite/

# playwright run output (Issue #17). The screenshots under artifacts/ are
# deliverables and stay tracked; everything here is regenerated on each run.
test-results/
playwright-report/
blob-report/
playwright/.cache/
